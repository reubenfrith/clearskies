# Copy this file to .env and fill in your values.
# NEVER commit .env — it is gitignored.

# FastAPI backend URL (Railway deployment URL, or http://localhost:8000 for local dev)
VITE_API_URL=https://your-api.railway.app
VITE_APP_PASSWORD=<choose-a-secret-password>
